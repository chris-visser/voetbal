// import { api } from '~~/convex/_generated/api'

export const useSponsors = () => {
  const convex = inject('convex-client')
  console.log(convex)
  //   const { data, suspense } = useConvexQuery(api.sponsors.get, {})
}
